# Official Documentation Snapshots

These Markdown files are downloaded from official OpenAI documentation pages by appending `.md` to their canonical documentation URLs, as documented by OpenAI.

They are stored as retrieved. `upstream/provenance/snapshot.json` records the source URL and hash for every file.

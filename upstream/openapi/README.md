# Official OpenAPI Snapshot

`openapi.yaml` is fetched byte-for-byte from the pinned `openai/openai-openapi` revision declared in `VERSION_LOCK.json`.

Do not hand-edit it. Change the pinned revision deliberately, rerun synchronization, and review the resulting diff.

# Codex Hooks Boundary

Codex Hooks are documented as lifecycle hooks in the Codex runtime. They must not be silently represented as a generic Responses API `tool` type unless the pinned official Responses schema explicitly establishes such a type.

For Responses work, consult the full OpenAPI schema first. For Codex lifecycle behavior, consult `upstream/docs/codex-hooks.md`.

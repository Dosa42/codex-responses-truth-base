# Reference Layer

This directory exists to make the official upstream material easy for humans and AI agents to navigate. It is derived orientation, not a competing source of truth.

Every factual claim here must be checkable against `upstream/openapi/openapi.yaml` or `upstream/docs/`.

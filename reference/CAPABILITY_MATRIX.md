# Capability Matrix

The machine-readable matrix is `CAPABILITY_MATRIX.json`. Its initial values are derived from the official GPT-5.6 Sol model page snapshotted on 2026-08-25.

Do not treat this matrix as timeless. Model capabilities can change; the snapshotted model documentation is the authority for this repository version.

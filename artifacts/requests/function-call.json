{
  "model": "gpt-5.6-sol",
  "input": "Use the supplied function when it is required.",
  "tools": [
    {
      "type": "function",
      "name": "example_lookup",
      "description": "Template custom function. Replace with a consuming project's real contract.",
      "parameters": {
        "type": "object",
        "properties": {"query": {"type": "string"}},
        "required": ["query"],
        "additionalProperties": false
      },
      "strict": true
    }
  ]
}

# Fixtures

Fixtures are structural examples for testing reusable parsers and integration code. They are not authoritative API documentation and must not be used to infer fields absent from `upstream/`.

# Skills Reference

Use the official Skill resource schemas/endpoints in `upstream/openapi/openapi.yaml` together with `upstream/docs/skills.md`.

A Skill resource, a Skill reference mounted in an execution environment, a Plugin package, an MCP server, and a custom function tool are distinct concepts. Do not flatten them into one abstraction when deriving API behavior.
